<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="checkBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/theme_gray</item>
        <item name="colorControlActivated">@color/theme_green</item>
    </style>

    <style name="RedButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="colorButtonNormal">@android:color/holo_red_light</item>
    </style>

    <style name="dialog_editText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/theme_green</item>

    </style>

    <style name="connect_loding" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogTitleText">@string/connect_and_send</item>
        <item name="DialogSpotColor">@android:color/holo_orange_dark</item>
        <item name="DialogSpotCount">6</item>
    </style>

</resources>
